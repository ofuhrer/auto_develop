release_id: planner-backend-1
active_objective: Add a strong-model planning workflow that generates, validates, and optionally writes bounded task contract drafts.
current_tasks:
  - pb-0001
  - pb-0002
  - pb-0003
  - pb-0004
next_steps:
  - Run the release queue in sequential mode without finalization.
  - Inspect evidence for each accepted task before trying autonomous finalization.
  - If the run is clean, repeat with accepted-task merge enabled on a clean branch.
