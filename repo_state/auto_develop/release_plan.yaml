release_id: runtime-supervisor-repair-loop
active_objective: Add a runtime supervisor that observes release events and evidence, diagnoses recoverable failures, applies bounded repair actions, and resumes execution without routine human intervention.
current_tasks:
  - runtime-supervisor-events
  - runtime-supervisor-policy-actions
  - runtime-supervisor-repair-executors
  - runtime-supervisor-resume-loop
  - runtime-supervisor-docs-state
next_steps:
  - Keep the structured release event stream, retry budgets, repair evidence, and stop reasons aligned with the shipped runtime-supervisor behavior.
  - Preserve the implemented repair/resume seam while the broader N-epic governor loop remains planned until it is actually implemented.
  - Identify procedural heuristics that can be retired once supervisor actions and the broader governor loop exist: deterministic backlog scoring, contract wording checks, log-pattern failure classification, budget-tuning prose, overlap recovery branching, and cockpit-summary filtering.
  - Keep deterministic gates authoritative; the supervisor must repair inputs and rerun gates, not bypass them.
  - Document the shipped runtime supervisor behavior while keeping the multi-epic governor loop planned.
