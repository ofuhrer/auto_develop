release_id: roadmap-governor-bootstrap
active_objective: Bootstrap autonomous roadmap-governor development for auto_develop.
current_tasks: []
next_steps:
  - Run plan-backlog in strong-model mode with --execute-planner and --write-objective.
  - Run run-objective on the generated objective once the governor-selected epic is acceptable under repository policy.
  - After the release, update backlog_state.yaml, release_plan.yaml, and roadmap/backlog docs from release evidence.
