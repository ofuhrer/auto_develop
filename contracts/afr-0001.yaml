task_id: afr-0001
release_id: agentic-feature-review-loop
title: Define feature review evidence schema
task_type: code_only
budget_class: M
objective: Add feature-review models and JSON evidence helpers without wiring them
  into release execution yet.
allowed_files:
- src/agentic_devloop/models.py
- src/agentic_devloop/evidence.py
- tests/test_models.py
- tests/test_evidence.py
forbidden_changes:
- Do not change existing task review, soft-gate, verification, or release finalization
  behavior.
- Do not rename or remove existing model fields.
- Do not change public JSON shape for existing evidence artifacts.
- Do not add network calls or agent execution in this task.
required_evidence:
- git diff
- pytest output for tests/test_models.py and tests/test_evidence.py
- git diff --check output
- changed-files list
verification:
  commands:
  - /Users/fuhrer/Desktop/auto_develop/main/.venv/bin/python -m pytest tests/test_models.py
    tests/test_evidence.py
  - git diff --check
  profile: null
stop_conditions:
- Stop if schema changes require modifying existing persisted release_summary.json
  or release_metrics.json formats outside additive fields.
- Stop if the evidence writer cannot preserve inspectable JSON with deterministic
  ordering and explicit paths.
- Stop if implementation requires changing task contract validation semantics.
non_goals: []
scientific_assumptions: []
fixture_changes_allowed: false
tolerance_changes_allowed: false
benchmark_delta_required: false
remote_dispatch: null
depends_on: []
