task_id: multi-epic-run-governor-hardening-0001
release_id: multi-epic-run-governor-hardening
title: Persist per-cycle governor state refresh summaries
task_type: code_only
budget_class: M
objective: Extend the existing state-review snapshot path into a per-cycle governor
  state-refresh summary artifact and thread its path into backlog planning and governor
  cycle results before epic selection.
allowed_files:
- src/agentic_devloop/models.py
- src/agentic_devloop/state_review.py
- src/agentic_devloop/backlog.py
- src/agentic_devloop/governor.py
- tests/test_state_store.py
- tests/test_backlog.py
forbidden_changes:
- Do not replace the existing StateReviewSnapshot artifact or break current state_review_snapshot_path
  fields.
- Do not add network access, external service calls, or non-deterministic artifact
  discovery beyond local repository, repo_state, and runs_dir inspection.
- Do not change release execution, finalization, cleanup, or feature-review behavior
  in this task.
- Do not broaden backlog planner prompt scope beyond adding the new state-refresh
  summary path and concise summary fields.
required_evidence:
- git diff
- pytest output for state refresh and governor/backlog tests
- serialized sample state-refresh artifact from a test run
verification:
  commands:
  - PYTHONPATH=src /Users/fuhrer/Desktop/auto_develop/main/.venv/bin/python -m pytest
    tests/test_backlog.py tests/test_state_store.py
  - git diff --check
  profile: null
stop_conditions:
- Stop if the state refresh summary cannot be generated before selecting an epic in
  each cycle.
- Stop if summary collection requires reading files outside repo_path, configured
  repo_state_path, or runs_dir.
- Stop if the implementation requires changing release execution semantics or finalization
  behavior.
- Stop if tests require edits outside the allowed files.
non_goals:
- Do not implement stop taxonomy, finalization handoff, or cleanup hooks here.
- Do not perform semantic agent review of the refreshed state.
- Do not update user documentation in this task.
scientific_assumptions: []
fixture_changes_allowed: false
tolerance_changes_allowed: false
benchmark_delta_required: false
remote_dispatch: null
depends_on: []
