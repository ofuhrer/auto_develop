task_id: supervisor-execution-strategy-0001
release_id: supervisor-execution-strategy
title: Add typed supervisor execution strategy decision model
task_type: code_only
budget_class: M
objective: Extend supervisor decision models with an execution-strategy decision type
  and tests for allowed actions, invalid action/outcome combinations, version validation,
  artifact write/load behavior, required rationale/fallback/validators, and missing
  evidence rejection.
allowed_files:
- src/agentic_devloop/supervisor_decisions.py
- tests/test_supervisor_decisions.py
- tests/test_supervisor_decision_artifacts.py
forbidden_changes:
- Do not change existing release scheduling, repair loop, review adjudication, budget,
  contract normalization, or environment repair decision semantics except to include
  the new union member.
- Do not relax schema_version validation or StrictModel extra-field rejection.
- Do not weaken evidence path containment or missing-evidence validation.
- Do not edit CLI, objective, planning, backlog, release execution, docs, lockfiles,
  generated artifacts, or runtime evidence.
required_evidence:
- git diff
- pytest output for supervisor decision tests
- artifact reload validation output
verification:
  commands:
  - PYTHONPATH=src /Users/fuhrer/Desktop/auto_develop/main/.venv/bin/python -m pytest
    tests/test_supervisor_decisions.py tests/test_supervisor_decision_artifacts.py
  - git diff --check
  profile: null
stop_conditions:
- Stop if the new execution-strategy model cannot be represented as a strict versioned
  supervisor decision without weakening existing decision validation.
- Stop if artifact reload validation requires broadening evidence path rules or allowing
  missing evidence.
- Stop if tests require edits outside the allowed files.
non_goals: []
scientific_assumptions: []
fixture_changes_allowed: false
tolerance_changes_allowed: false
benchmark_delta_required: false
remote_dispatch: null
depends_on: []
